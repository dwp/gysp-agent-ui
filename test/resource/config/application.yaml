env: local
secret: thisIsASecret
application:
  port: 8100
  session:
      store: redis
      name: sessionID
      secret: thisIsASecret
      timeout: 1800000
      securecookies: false
  urls:
      agentGateway: http://example.com/api
      apiKey:
      batchApiKey:
      awardApiKey :
      addressLookupApiKey:
      paymentApiKey:
      hmrcCalculationApiKey:
      host: host
  robot:
    key:
  logs:
      level: error
      path: logs
      size: 10240k
      backups: 10
      rotate: daily
  redis:
      host: localhost
      port: 6379
      prefix: spa
      password: thisIsASecret
  kong:
      enabled: false
